<?xml version="1.0" encoding="UTF-8" ?>
<!-- 日志级别从低到高分为TRACE < DEBUG < INFO < WARN < ERROR < FATAL，如果设置为WARN，则低于WARN的信息都不会输出 -->
<!-- scan:当此属性设置为true时，配置文档如果发生改变，将会被重新加载，默认值为true -->
<!-- scanPeriod:设置监测配置文档是否有修改的时间间隔，如果没有给出时间单位，默认单位是毫秒。
                 当scan为true时，此属性生效。默认的时间间隔为1分钟。 -->
<!-- debug:当此属性设置为true时，将打印出logback内部日志信息，实时查看logback运行状态。默认值为false。 -->
<configuration scan="true" scanPeriod="10 seconds" >
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <!-- 获取application中定义的属性。  -->
    <springProperty scope="context"
        name="springAppName"
        source="spring.application.name"/>

    <!-- 在springframework的defaults.xml引用文件中也定义下相关格式配置，此处会覆盖spring配置。  -->
    <property name="LOG_FILE"
        value="${springAppName}"/>

    <!-- 使用%clr彩色日志，可以增加日志辨识度，简化排查难度-->
    <property name="CONSOLE_LOG_PATTERN"
        value="${CONSOLE_LOG_PATTERN:-%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p})
              %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan}
              %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}}"/>

    <!-- 控制台输出 -->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <!-- 过滤日志级别，优先级高于root的全局设置 -->
            <level>INFO</level>
        </filter>
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>

    <!-- 按照每天生成日志文件 -->
    <appender name="filelog"  class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/${LOG_FILE}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!--日志文件输出的文件名-->
            <FileNamePattern>logs/${LOG_FILE}.%d{yyyy-MM-dd_HH}.%i.log</FileNamePattern>
            <!--日志文件保留天数-->
            <MaxHistory>7</MaxHistory>
            <MaxFileSize>20MB</MaxFileSize>
            <totalSizeCap>5GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符-->
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 按照每天生成日志文件 -->
    <appender name="systemLog"  class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/${LOG_FILE}.SYSTEMLOG.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!--日志文件输出的文件名-->
            <FileNamePattern>logs/${LOG_FILE}.%d{yyyy-MM-dd_HH}.%i.SYSTEMLOG.log</FileNamePattern>
            <!--日志文件保留天数-->
            <MaxHistory>7</MaxHistory>
            <MaxFileSize>20MB</MaxFileSize>
            <totalSizeCap>5GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符-->
            <pattern>%m%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <logger name="systemLog" level="INFO" additivity="false">
        <appender-ref ref="systemLog"/>
    </logger>

    <!-- 开发环境日志级别为DEBUG -->
    <springProfile name="dev">
        <root level="DEBUG">
            <appender-ref ref="console"/>
        </root>
        <root level="INFO">
            <appender-ref ref="filelog"/>
        </root>
    </springProfile>

    <!-- 测试环境日志级别为INFO -->
    <springProfile name="test">
        <root level="DEBUG">
            <appender-ref ref="console"/>
        </root>
        <root level="INFO">
            <appender-ref ref="filelog"/>
        </root>
    </springProfile>

    <!-- 生产环境日志级别为ERROR -->
    <springProfile name="prod">
        <root level="DEBUG">
            <appender-ref ref="console"/>
        </root>
        <root level="INFO">
            <appender-ref ref="filelog"/>
        </root>
    </springProfile>

    <springProfile name="demo">
        <root level="DEBUG">
            <appender-ref ref="console"/>
        </root>
        <root level="INFO">
            <appender-ref ref="filelog"/>
        </root>
    </springProfile>


</configuration>
